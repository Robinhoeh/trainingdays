{
  "name": "trainingdays",
  "version": "0.1.0",
  "homepage": "http://Robinhoeh.github.io/trainingdays",
  "private": true,
  "dependencies": {
    "react-scripts": "1.0.14"
  },
  "devDependencies": {
    "gh-pages": "1.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom"
  }
}
